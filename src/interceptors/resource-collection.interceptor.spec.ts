import { ResourceCollectionInterceptor } from './resource-collection.interceptor';

describe('ResourceCollectionInterceptor', () => {
  it('should be defined', () => {
    expect(new ResourceCollectionInterceptor()).toBeDefined();
  });
});
